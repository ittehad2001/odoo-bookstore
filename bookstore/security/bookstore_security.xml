<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="module_category_bookstore" model="ir.module.category">
        <field name="name">Bookstore</field>
        <field name="sequence">30</field>
    </record>

    <record id="res_groups_privilege_bookstore" model="res.groups.privilege">
        <field name="name">Bookstore</field>
        <field name="sequence">30</field>
        <field name="category_id" ref="module_category_bookstore"/>
    </record>

    <record id="group_bookstore_user" model="res.groups">
        <field name="name">User</field>
        <field name="sequence">10</field>
        <field name="privilege_id" ref="res_groups_privilege_bookstore"/>
        <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
        <field name="comment">Can manage books, authors, and sales (no delete).</field>
    </record>

    <record id="group_bookstore_manager" model="res.groups">
        <field name="name">Administrator</field>
        <field name="sequence">20</field>
        <field name="privilege_id" ref="res_groups_privilege_bookstore"/>
        <field name="implied_ids" eval="[(4, ref('group_bookstore_user'))]"/>
        <field name="user_ids" eval="[(4, ref('base.user_root')), (4, ref('base.user_admin'))]"/>
        <field name="comment">Full Bookstore access including delete.</field>
    </record>
</odoo>
